# Generate a strong token with: openssl rand -hex 32
ADMIN_TOKEN=REPLACE_WITH_LONG_RANDOM_TOKEN

PORT=4001

# Leave true if you put this behind HTTPS. Set to false only if you are
# accessing it over plain http://localhost through an SSH tunnel.
COOKIE_SECURE=false
